@using Web.Models

@{
    ViewBag.Title = "NivelIntermedio";
}
@section Levels
{
    <link rel="stylesheet" href="~/Content/Levels.css">

}
<!-- Rutina seleccionada -->
<div class="intro">
    <h1>
        Tu rutina consiste en completar 4 rondas de 4 <a href='@Url.Action("Funcional", "Home")'>ejercicios funcionales</a> los dias lunes miercoles y viernes.
    </h1>
    <p>
        Podras elejir los ejercicios a continuacion, donde se indicaran las repeticiones correspondientes.
        Al terminar las repeticiones indicadas puedes descansar 15 segundos y pasar al siguiente hasta completar todos los ejercicios de la ronda.
        Descansaras por un minuto y comenzaras la siguiente ronda repitiendo el proceso hasta completar 4.<br />
        <span>Importante:</span> en esta modalidad no se tiene en cuenta el tiempo que tardes en
        realizar los ejercicios, sino que <span>el objetivo es completar las rondas</span>.
    </p>
    <h3>
        Cuando vayas ganando condicion fisica podras suprimir los descansos entre ejercicios y aumentar lar repeticiones.
    </h3>
</div>

<!-- Eleccion de los ejercicios-->
<div class="container" id="eleccion">
    <div class="form-group">
        <form action="@Url.Action("RutinaIntermedia","Home")" , method="post">
            <div class="col-md-6 col-md-offset-3 col-sm-offset-3 col-sm-6">
                <label class="col-form-label">Ejercicio 1</label>
                <select class="form-control" name="Ejercicio1">
                    @foreach (Exercise exercise in @ViewBag.Exercises)
                    {
                        <option value="@exercise.ID">@exercise.Name</option>}
                </select>
            </div>
            <div class="col-md-6 col-md-offset-3 col-sm-offset-3 col-sm-6">
                <label class="col-form-label">Ejercicio 2</label>
                <select class="form-control" name="Ejercicio2">
                    @foreach (Exercise exercise in @ViewBag.Exercises)
                    {
                        <option value="@exercise.ID">@exercise.Name</option>}
                </select>
            </div>
            <div class="col-md-6 col-md-offset-3 col-sm-offset-3 col-sm-6">
                <label class="col-form-label">Ejercicio 3</label>
                <select class="form-control" name="Ejercicio3">
                    @foreach (Exercise exercise in @ViewBag.Exercises)
                    {
                        <option value="@exercise.ID">@exercise.Name</option>}
                </select>
            </div>
            <div class="col-md-6 col-md-offset-3 col-sm-offset-3 col-sm-6">
                <label class="col-form-label">Ejercicio 4</label>
                <select class="form-control" name="Ejercicio4">
                    @foreach (Exercise exercise in @ViewBag.Exercises)
                    {
                        <option value="@exercise.ID">@exercise.Name</option>}
                </select>
            </div>
            <div class="col-md-6 col-md-offset-3 col-sm-offset-3 col-sm-6">
                <button type="submit" class="btn btn-intermedio">Seleccionar</button>
            </div>
        </form>
    </div>
</div>

@section scripts{
    <script>
        $("ul").append("<li><a href='@Url.Action("NivelInicial", "Home")'>Nivel anterior</a></li><li><a href='@Url.Action("NivelAvanzado", "Home")'>Siguiente Nivel</a></li>")
    </script>
}
